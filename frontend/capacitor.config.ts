import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.autostructure.app',
  appName: 'Auto Structure Analysis',
  webDir: 'dist'
}

export default config
